# OPT4001 Hardening Finding-to-Prompt Plan

Date: 2026-06-02
Branch: `hardening/opt4001-industry-readiness`
Baseline commit: `4e76a03a19e6644ff6a0941018ef55ceb83de754`
Source audit report: `docs/OPT4001_INDUSTRY_READINESS_EXPLORATION_REPORT.md`
Prompt: Prompt 1 - branch, baseline, AGENTS.md, and finding-to-prompt plan

Prompt 1 implemented no functional fixes. The only intended changes are this planning document and an `AGENTS.md` rules update for future hardening prompts.

## Baseline Identity

Required first commands:

| Command | Result |
| --- | --- |
| `git status --short` | Clean output before edits. |
| `git branch --show-current` | `main` before branch creation; `hardening/opt4001-industry-readiness` after branch creation. |
| `git log --oneline -5` | `4e76a03 feat: implement comprehensive prompts for OPT4001 industry readiness hardening`; `2c4dca6 docs: add prompt sequence README for OPT4001 industry-readiness hardening`; `a91180a Merge pull request #1 from janhavelka:idf-port`; `8464d41 feat: Add vfs requirement and update idf_component.yml with target specifications`; `55716d0 Enforce native ESP-IDF example boundary`. |
| `git checkout -b hardening/opt4001-industry-readiness` | Created and switched to the hardening branch. |

## Subagent Confirmation Summary

| Subagent | Short result |
| --- | --- |
| finding-index | Extracted H1-H5, M1-M8, and L1-L4 from the exploration report and mapped them to later prompts with done criteria. |
| core-contracts | Confirmed lifecycle, implicit copy/move, uninitialized raw register access, bounded blocking loops, `nowMs` dependency, `tick()` I2C behavior, repeated sample availability, and missing thread/ISR/reentrancy contracts. |
| device-correctness | Confirmed package/address rules, DEVICE_ID nuance, result/counter/CRC format, CRC vector gap, flags clear behavior, readiness risk, FIFO ordering inconsistency, threshold overflow, and partial INT semantics. |
| build-ci | Confirmed `Version.h` is included by public API, ignored by git, generated only by PlatformIO, not generated by root CMake, and not covered by pure ESP-IDF CI. |
| docs-examples | Confirmed production/ESP-IDF/validation claims exceed evidence, `SECURITY.md` is stale, and examples need clearer diagnostic/bring-up labeling. |

## Prompt Sequence

| Prompt | Scope | Primary findings |
| --- | --- | --- |
| Prompt 2 | Clean checkout and build reproducibility | H1 |
| Prompt 3 | Lifecycle/public API contracts | H2, copy/move, thread/ISR/reentrancy contracts, M6, L2 |
| Prompt 4 | Probe, device ID, and diagnostic precision | H4 |
| Prompt 5 | Fresh sample/readiness and blocking semantics | H3, blocking/readiness `nowMs` and yield behavior |
| Prompt 6 | Numeric correctness and independent vectors | M1, M2, M8, missing lux/CRC/threshold/range/conversion-time vectors |
| Prompt 7 | FIFO, INT/threshold details, and partial-state consistency | M3, M4, INT/threshold/flags semantic gaps |
| Prompt 8 | Documentation honesty, metadata, examples, and non-hardware CI polish | H5 documentation portion, M6 documentation portion, M7 docs portion, L1, L3, L4 |
| Prompt 9 | Hardware, optical, address-pin, INT, FIFO, fault, and pure ESP-IDF validation | H5 validation portion, M5, M7 runtime portion, hardware validation gaps |

## Finding-to-Prompt Mapping

| Finding | Later prompt | Evidence anchors | Done criteria |
| --- | --- | --- | --- |
| H1. Fresh checkout/manual/ESP-IDF builds are not reproducible | Prompt 2 | `include/OPT4001/OPT4001.h:11`, `.gitignore:41`, `platformio.ini:8`, `CMakeLists.txt:1`, `README.md:59`, `README.md:63` | A clean checkout can build manual/native and ESP-IDF consumers without relying on an ignored local `Version.h`; CI proves the path. |
| H2. Public raw register APIs can touch I2C while driver is uninitialized | Prompt 3 | `include/OPT4001/OPT4001.h:280`, `src/OPT4001.cpp:1444`, `src/OPT4001.cpp:1457` | Public single-register APIs return `NOT_INITIALIZED` outside the lifecycle and tests prove no bus I/O occurs. |
| Copy/move not deleted | Prompt 3 | `include/OPT4001/OPT4001.h:125`, `include/OPT4001/OPT4001.h:349` | Driver copy/move operations are explicitly deleted or intentionally defined and documented. |
| Missing thread/ISR/reentrancy public contracts | Prompts 3 and 8 | `include/OPT4001/OPT4001.h:125`, `Config.h:12`, `README.md:154` | Public Doxygen and README state non-thread-safe, non-ISR-safe, external serialization, and no callback re-entry contracts. |
| H4. Probe and device-ID diagnostics are too weak for production startup | Prompt 4 | `src/OPT4001.cpp:253`, `src/OPT4001.cpp:262`, `src/OPT4001.cpp:284`, `src/OPT4001.cpp:1473`, `docs/OPT4001_datasheet.md:353` | Probe/recover preserve transport detail and validate fixed DEVICE_ID fields with NACK, timeout, bus error, and bad-ID tests. |
| H3. Continuous/readiness state can over-report stale or premature samples | Prompt 5 | `src/OPT4001.cpp:447`, `src/OPT4001.cpp:526`, `docs/OPT4001_datasheet.md:784`, `docs/OPT4001_datasheet.md:433` | Fresh reads are gated by `CONVERSION_READY_FLAG`, INT, or counter changes, and repeated stale reads are distinguishable. |
| Blocking/readiness `nowMs`/yield behavior | Prompt 5 | `Config.h:24`, `src/OPT4001.cpp:99`, `src/OPT4001.cpp:724`, `src/OPT4001.cpp:1997` | Blocking helpers document and enforce timebase/yield behavior, including no-timebase and stalled-clock tests. |
| Threshold/raw lux overflow and undefined shifts | Prompt 6 | `src/OPT4001.cpp:1522`, `src/OPT4001.cpp:1656`, `src/OPT4001.cpp:1963`, `docs/OPT4001_datasheet.md:560` | Numeric paths validate bounds or widen intermediates so legal inputs cannot overflow or shift into undefined behavior. |
| Missing independent lux/CRC/threshold/range/conversion-time vectors | Prompt 6 | `test/test_basic.cpp:194`, `docs/OPT4001_datasheet.md:499`, `docs/OPT4001_datasheet.md:522`, `docs/OPT4001_datasheet.md:560` | Tests include independent vectors or a test-side oracle for lux, CRC, threshold encoding, ranges, and conversion-time tables. |
| FIFO CRC/freshness/ordering gaps | Prompt 7 | `src/OPT4001.cpp:587`, `docs/OPT4001_datasheet.md:630`, `docs/OPT4001_datasheet.md:644`, `docs/OPT4001_datasheet.md:649` | FIFO order is documented consistently and burst reads expose per-slot CRC/freshness without losing later valid slots. |
| Multi-register partial-state/cache dirty tracking | Prompt 7 | `src/OPT4001.cpp:1091`, `src/OPT4001.cpp:1853`, `src/OPT4001.cpp:1792` | Partial write failures either cannot desynchronize cache/hardware or expose dirty/resync-required state with recovery tests. |
| INT/threshold/flags semantics and hardware validation gap | Prompts 7 and 9 | `src/OPT4001.cpp:903`, `src/OPT4001.cpp:1262`, `docs/OPT4001_datasheet.md:401`, `docs/OPT4001_datasheet.md:608`, `docs/OPT4001_datasheet.md:619` | Register semantics are package-gated and tested in fake bus; hardware validation records threshold, INT, and flag behavior. |
| H5. Production/validation claims exceed evidence | Prompts 8 and 9 | `README.md:3`, `README.md:356`, `library.json:4`, `idf_component.yml:2`, `docs/IDF_PORT_IMPLEMENTATION.md:3` | Claims are downgraded or backed by committed validation logs for hardware, optical, INT, FIFO, address, fault, and IDF paths. |
| ESP-IDF CI/build readiness and loose include boundary | Prompt 8 | `.github/workflows/ci.yml:41`, `tools/check_idf_example_contract.py:113`, `CMakeLists.txt:1` | CI runs IDF contract checks and the include/build boundary is documented before pure IDF validation in Prompt 9. |
| ESP-IDF CLI blocking `tick()` | Prompt 8 | `README.md:67`, `examples/esp_idf/basic/main/main.cpp:383`, `examples/esp_idf/basic/main/main.cpp:430` | Docs stop overstating parity and the CLI either avoids blocking periodic work or states it is diagnostic-only. |
| SECURITY.md and metadata stale | Prompt 8 | `SECURITY.md:7`, `library.json:3`, `idf_component.yml:1` | Security/support metadata matches current package version and a consistency check or release rule prevents drift. |
| Local documentation summary inconsistencies | Prompt 8 | `docs/OPT4001_datasheet.md:285`, `docs/OPT4001_datasheet.md:644`, `docs/AN_picostar_package.md:28` | Conflicting FIFO and package/temperature summaries are corrected or explicitly marked as unresolved source issues. |
| Example transports are not production shared-bus examples | Prompt 8 | `examples/01_basic_bringup_cli/main.cpp:2`, `examples/esp_idf/basic/main/main.cpp:1`, `examples/common/BoardConfig.h:6` | Examples and adapters are labeled diagnostic/bring-up and shared-bus locking guidance is documented. |
| Hardware/optical/address/INT/FIFO/fault validation pending | Prompt 9 | `docs/OPT4001_INDUSTRY_READINESS_EXPLORATION_REPORT.md:342`, `docs/OPT4001_INDUSTRY_READINESS_EXPLORATION_REPORT.md:563` | Validation logs capture board, package, firmware hash, commands, expected results, observed results, and failures. |

## Prompt 2 Status Update

H1 status: complete for source-level clean-checkout, manual include, and
ESP-IDF/CMake public-header reproducibility.

Evidence:

- `.gitignore` no longer ignores `include/OPT4001/Version.h`.
- `include/OPT4001/Version.h` is staged/tracked as a generated header derived
  from `library.json`.
- `scripts/generate_version.py check` reports the tracked header is up to date.
- `tools/check_version_header_contract.py` verifies public include resolution,
  tracked-header status, `library.json` version consistency, and generator
  freshness.
- CI runs `tools/check_version_header_contract.py` before PlatformIO can mask a
  missing generated header.
- README and ESP-IDF docs document the tracked header and verification commands.

Remaining limitation:

- Real pure ESP-IDF builds are still not locally validated because `idf.py` is
  not available on `PATH` in this shell. Prompt 9 remains responsible for full
  pure ESP-IDF and hardware validation evidence.

## Prompt 3 Status Update

H2 status: complete for public raw-register lifecycle guards and object-model
contracts.

Evidence:

- `readRegister16()` and `writeRegister16()` now return `NOT_INITIALIZED`
  without I2C while `UNINIT`.
- Internal register access uses private tracked helpers so `begin()`, `recover()`,
  and reset/reapply paths do not depend on public lifecycle APIs.
- Normal public raw-register access returns `BUSY` without I2C while `OFFLINE`,
  matching the documented normal public-operation policy.
- `OPT4001` is default constructible but copy construction, copy assignment, move
  construction, and move assignment are deleted.
- Public headers now document lifecycle, offline, blocking, thread-safety,
  ISR-safety, callback non-reentrancy, and shared-bus serialization contracts.
- Native tests cover public raw-register access before `begin()`, after failed
  `begin()`, after `end()`, while `OFFLINE`, compile-time object traits, and
  `probe()` raw transport behavior without requiring an initialized lifecycle.

Remaining limitation:

- `end()` remains a compatibility best-effort `void` API; Prompt 3 documents
  the behavior rather than adding a new status-returning shutdown API.

## Prompt 4 Status Update

H4 status: complete for source-level probe, `begin()`, `recover()`, and decoded
device-ID diagnostics.

Evidence:

- `CommandTable.h` now exposes the full `DEVICE_ID` fixed-bit/DIDL/DIDH layout:
  reserved bits `[15:14]`, `DIDL[13:12]`, and `DIDH[11:0]`.
- `decodeDeviceId()` reports `reservedBitsClear` and sets `matchesExpected` only
  when reserved bits are clear, `DIDL == 0`, and `DIDH == 0x121`.
- `probe()`, `begin()` through `probe()`, and `recover()` use the same strict
  validation helper, so values such as `0x1121` and `0x4121` no longer pass just
  because low `DIDH` matches.
- `probe()` returns raw transport statuses such as `I2C_NACK_ADDR`,
  `I2C_NACK_DATA`, `I2C_TIMEOUT`, `I2C_BUS`, and `I2C_ERROR` instead of
  collapsing them to `DEVICE_NOT_FOUND`.
- Native tests cover valid full ID, wrong DIDL/fixed bits in `probe()`,
  `begin()`, and `recover()`, preserved probe transport statuses, decode helper
  semantics, and the no-health-side-effect raw probe contract.
- README and ESP-IDF port docs document the startup status mapping and the
  ESP-IDF limitation that NACK phase is not exposed by the transaction API used
  in the example adapter.

Remaining limitation:

- Prompt 4 improves the ESP-IDF adapter mapping and documentation, but real
  target-IDF and hardware validation remain in Prompt 9.

## Prompt 5 Status Update

H3 status: complete for source-level freshness/readiness semantics and bounded
blocking contracts.

Evidence:

- `tick()` and `conversionReady()` no longer mark samples ready from elapsed time
  alone; elapsed time is only a gate for hardware-evidence polling.
- Freshness is now defined by `CONVERSION_READY_FLAG`, configured SOT-5X3 INT
  assertion, or sample-counter advance from the previous accepted fresh sample.
- `readSample()`, `readBurst()`, `tryReadSample()`, `tryReadFreshSample()`, and
  `readFreshBlocking()` consume fresh readiness on `OK` or `CRC_ERROR`, so
  repeated reads of the same counter do not report a new sample.
- `readLatestSample()` provides explicit current-register access for callers
  that want latest data without freshness proof.
- Blocking reads now require `Config::nowMs` before starting a conversion, while
  retaining a finite poll cap as a guardrail.
- Native tests cover continuous first-fresh, duplicate counter rejection,
  counter wrap `15 -> 0`, same lux with changed counter, one-shot flag gating,
  forced-auto early timeout, stale one-shot-to-continuous transitions, missing
  `nowMs`, and CRC-on-fresh consumption.
- README and public Doxygen document latest/fresh/cached definitions, counter
  wrap behavior, flag clear side effects, auto-range timing, blocking timebase
  requirements, and INT/PicoStar/SOT-5X3 ownership boundaries.

Remaining limitation:

- Hardware timing, INT pulse behavior, and pure ESP-IDF runtime validation remain
  pending for Prompt 9.

## Prompt 6 Status Update

M1/M2/M8 numeric-vector status: complete for source-level numeric conversion
safety and independent native vectors.

Evidence:

- Raw result conversions now validate exponent `0..8` and 20-bit mantissa
  bounds before shifting, use `uint64_t` intermediates, and return
  `INVALID_PARAM` through status-returning helpers. Legacy float helpers return
  quiet NaN for invalid raw input instead of invoking undefined shifts.
- Sample decode rejects impossible result exponents before caching scaled lux.
- Threshold ADC-code conversion now has a lossless `uint64_t` overload; the
  legacy `uint32_t` helper saturates at `UINT32_MAX` instead of wrapping.
- Threshold lux packing rejects negative, non-finite, and out-of-range lux
  values and uses nearest-code rounding before register quantization.
- Threshold interrupt ordering uses 64-bit ADC-code comparison, so high-exponent
  reversed windows are rejected.
- Native tests include independent lux/result vectors for PicoStar and SOT-5X3,
  invalid raw exponent/mantissa vectors, invalid decoded exponent/no-cache
  behavior, 64-bit threshold vectors, high-exponent threshold ordering,
  datasheet-side CRC vectors that do not call `_computeCrcNibble()`, every
  conversion-time table entry, full package/address matrix checks, and valid /
  invalid range/config vectors.
- README and public Doxygen document lux units, package scaling, result and
  threshold bounds, threshold rounding/range policy, CRC mismatch behavior,
  conversion-time table, package/address matrix, and VDD/I/O voltage warning.

Remaining limitation:

- FIFO per-slot CRC/ordering visibility, INT behavior, and partial-state
  consistency remain assigned to Prompt 7. Hardware and pure ESP-IDF target
  validation remain assigned to Prompt 9.

## Prompt 2 Scope

Prompt 2 should fix H1 only: make `Version.h` or an equivalent version source reproducible for clean manual/native/CMake and ESP-IDF consumers. It should not address lifecycle, probe, sample readiness, numeric, FIFO, docs honesty, metadata, or hardware validation unless directly required by the reproducibility fix.

Suggested Prompt 2 done criteria:

- Fresh checkout with ignored/generated files removed can compile a minimal consumer including `OPT4001/OPT4001.h`.
- Root CMake/ESP-IDF consumption no longer depends on a pre-existing ignored `include/OPT4001/Version.h`.
- `scripts/generate_version.py check` remains useful and deterministic.
- CI or a local reproducibility check covers the clean-checkout path.
- No public API behavior changes are introduced.

## Baseline Checks Attempted

| Command | Exact result |
| --- | --- |
| `python --version` | `Python 3.12.10` |
| `python -m platformio --version` | `PlatformIO Core, version 6.1.18` |
| `python tools\check_core_timing_guard.py` | `Core framework guard PASSED` |
| `python tools\check_cli_contract.py` | `CLI contract PASSED` |
| `python tools\check_idf_example_contract.py` | `IDF example contract PASSED` |
| `python scripts\generate_version.py check` | `Up to date: C:\Users\Honza\Documents\Projects\OPT4001\include\OPT4001\Version.h` |
| `python -m platformio test -e native` | Passed; `50 test cases: 50 succeeded in 00:00:03.320`; PlatformIO printed obsolete-core warning for v6.1.18. |
| `python -m platformio run -e esp32s3dev` | Passed; `esp32s3dev SUCCESS`; RAM `6.9%`, Flash `33.1%`; PlatformIO printed obsolete-core warning for v6.1.18. |
| `python -m platformio run -e esp32s2dev` | Passed; `esp32s2dev SUCCESS`; RAM `11.3%`, Flash `32.5%`; PlatformIO printed obsolete-core warning for v6.1.18. |
| `python -m platformio pkg pack` | Passed; wrote `OPT4001-1.0.0.tar.gz`; artifact was removed and is not intended for commit. |
| `idf.py --version` | Failed: `idf.py : The term 'idf.py' is not recognized as the name of a cmdlet, function, script file, or operable program.` |

## Commands Not Run

Pure ESP-IDF `idf.py -C examples/esp_idf/basic set-target ... build` commands were not run because `idf.py --version` failed: ESP-IDF is not installed or not on `PATH` in this shell.

Hardware validation commands were not run in Prompt 1 because no real OPT4001 hardware, logic analyzer captures, reference lux meter data, or hardware logs were available.

## Prompt 1 Done Criteria

- Hardening branch exists: done.
- Baseline command evidence captured: done.
- Requested subagents spawned and summarized: done.
- `AGENTS.md` updated with Prompt 1 coding-agent rules: done.
- Finding-to-prompt mapping created: done.
- No functional fixes implemented: done.
